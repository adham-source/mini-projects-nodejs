extends ../layouts/layout.pug 
block content 
    h2 New Book 
    form(action="/books", method="POST")
        div 
            include _formFields.pug
        div(class="form-row-end btn-row")
            a(href="/books", class="btn btn-danger") Cancel 
            button(type="submit", class="btn btn-primary") Create 